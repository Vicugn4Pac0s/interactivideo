{
  "name": "interactivideo",
  "private": false,
  "version": "0.1.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "prepublish": "rimraf dist && $(npm bin)/tsc",
    "dev": "vite",
    "build": "npm run clean && tsc",
    "preview": "vite preview"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "~5.7.2",
    "vite": "^6.1.0"
  },
  "peerDependencies": {
    "gsap": "^3.12.7"
  }
}
